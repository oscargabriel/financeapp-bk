package com.oscargabriel.financeapp.domain.model;

/** Los mismos valores que ck_transactions_type. El signo del monto lo da el tipo, nunca el monto. */
public enum TransactionType {
    EXPENSE, INCOME, TRANSFER
}
